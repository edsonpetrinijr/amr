# 2026-06-08

## Resumo do dia
- **Aconteceu:** Decodificamos o feed de pedidos da CAT (txt largura-fixa + campo de observação) e achamos as fontes reais (txt feed + tabelas Oracle). Alinhamento com Zampim (licença Fleet RDS p/ teste; marca "AMR" na observação do pedido). Plano do MVP da linha de montagem (transmissão). Polimento pré-demo do app operador: 100% pt-BR, branding unificado "Caterpillar Inc. Fleet", headers uniformes, fixes (mac spacer, "BehaveX" no runtime).
- **Decidido:** Roteamento por flag na observação do pedido (fonte da verdade = sistema da CAT, sem lista paralela). Postura: observador passivo read-only (txt = gatilho; Oracle = enriquecimento). UI pt-BR, código em inglês. Não dar baixa no carregamento (preserva rastreabilidade).
- **Próximo:** **Reposição PoC verificado no SIM (verde).** Founder confirma 2 premissas: estações SIM (ENVIO=CB-ALMOX/POU=CB1/RECEB=CB-ALMOX) e semântica do cap (por-poll vs. total). Confirmar layout/posições do txt com Cleber; schema/chave Oracle com Douglas; cronograma do DOS com Fábio.

## Log
_Entradas históricas (importadas da TIMELINE.md monolítica). Horários por ação passam a ser registrados a partir de agora._

### 2026-06-08 ~10:58 — Reposição VERIFICADO ponta-a-ponta no SIM (+1 bug de dispatcher corrigido)
- **Done:** Passada de auditoria-e-verificação (build já estava feito). **Verificação end-to-end no SIM real:** adapter 1º poll `lines=360956 order=175328 matched=28571 new=5` → 5 linhas `C ILC` como `ready_for_confirmation` (pickup `CB-ALMOX`/dropoff `CB1`, `amr_flagged=true`); **re-poll NÃO duplica** (mesmo `order_key`). **Confirmar entrega (live):** transição observada **`ready_for_confirmation → dispatched → delivered`**, robô andou `IDLE→ENROUTE_PICKUP(CB-ALMOX)→AT_PICKUP→ENROUTE_DROP(CB1)→DONE→RETURNING→IDLE`, SSE `assigned→at_pickup→enroute_drop→done`. **Buscar vazio (live):** despacho `CB1→CB-ALMOX` (T0003), robô andou. **Bug corrigido (1 arquivo):** `server/app/dispatcher.py` ganhou `_settle_returning()` (RETURNING→IDLE na chegada) — sem isso o único robô SIM nunca liberava e o 2º job ficava `pending` p/ sempre.
- **Decided:** Cap=5 é **por-poll de linhas NOVAS** (throttle por ciclo), não total pendente — fila cresce +5/poll. Confirmar com founder se é o comportamento desejado (vs. cap-total-outstanding). Mapa SIM (`erp_mapping.yaml`): `C ILC`→pickup `CB-ALMOX`(LM1/Almox), dropoff `CB1`(LM2/Linha A), recebimento `CB-ALMOX` — únicos 2 landmarks no `1007.smap`.
- **Next:** **Founder confirmar 2 premissas:** (1) estações SIM ENVIO/POU/RECEBIMENTO acima; (2) semântica do cap (por-poll vs. total). Follow-ups deferidos: consolidar `Reposicao.tsx` p/ ler o slice `erpOrders` do store (hoje tem SSE próprio); senior-engineer revisar o fix cross-cutting do `dispatcher.py`; corrigir teste pré-existente `test_stop_all` (artefato asyncio Py3.13, não-ERP).
- **Tests:** **pytest 81 passed / 1 failed** (a falha é pré-existente e não-ERP: `test_operator_endpoints::test_stop_all_...`, asyncio Py3.13). **tsc 0 erros, eslint 0 erros/0 warnings.**
- **Refs:** `server/app/erp/*`, `server/app/dispatcher.py` (`_settle_returning`), `server/config/erp_mapping.yaml`, `desktop/app/pages/Reposicao.tsx`.

### 2026-06-08 ~10:55 — Fluxo "Reposição" construído ponta a ponta (adapter F1 + board + 2 callbuttons)
- **Done:** **Backend** (`server/app/erp/`: parser, filter, mapping, reconcile, service) ligado ao `main.py` — poller thread lê o feed (default `TRANVR2.TXT`, copy-then-parse, nunca abre o original), parse largura-fixa do registro de pedido, dedup por `order_key` (sha1 content-based) em `erp_order`/`erp_snapshot` no `fleet.db`, mapa POU→station via config. Endpoints `GET /erp/orders`, `POST /erp/confirm-delivery`, `POST /erp/request-empty`; broadcast SSE `{"type":"erp_order"}`. OPC UA callbutton driver recebe `action_handler=_erp_service.handle_action` (os 2 botões). **Frontend** novo board `desktop/app/pages/Reposicao.tsx` (fila FIFO, lane de vazios, indicador AMR-pronto no ENVIO, botões "Confirmar entrega"/"Buscar vazio", badges de status pt-BR, SSE-refresh + fallback 5s) + rota + nav "Reposição" (ícone PackageCheck); API client/types já existiam.
- **Decided:** **Dispatch confirmation-gated, FIFO** — pedidos com a tag entram como `ready_for_confirmation` e NÃO auto-dispatcham; o callbutton 1 confirma o mais antigo e despacha ENVIO→POU; callbutton 2 manda buscar vazio POU→RECEBIMENTO. **Filtro de teste = `cell == "C ILC"`** (observação está em branco em 99% dos pedidos; "PAULO" só aparece em atendimento, não em pedido — inservível). Filtro é **config** (`ERP_AMR_FILTER`) → troca p/ observação="AMR" numa linha quando a marca existir. Cap de dispatch (`ERP_MAX_DISPATCH`, default ~5) p/ não criar 28k jobs no teste.
- **Next:** **Founder roda `npm run lint` + `npm run build` (tsc)** p/ confirmar verde — shell ficou bloqueado nesta sessão, não consegui validar/commitar. Depois: smoke test ponta-a-ponta no SIM (feed→`erp_order`→confirm→robô move no Sim→board atualiza via SSE). Watcher de cadência agora é tagarela (heartbeat por poll, `--log-file`, banner de change). Quando a marca real "AMR" e o token forem confirmados c/ Cleber, trocar `ERP_AMR_FILTER` p/ observação.
- **Refs:** `server/app/erp/*`, `server/app/main.py` (rotas `/erp/*` + poller + action_handler), `desktop/app/pages/Reposicao.tsx`, `desktop/app/routes.tsx`, `desktop/app/components/Layout.tsx`, `scripts/feed_watcher/feed_watcher.py` (verbose).

### 2026-06-08 ~09:40 — Adapter ERP desenhado (CTO) + watcher de cadência entregue
- **Done:** (1) **Watcher de cadência** `scripts/feed_watcher/feed_watcher.py` (stdlib, testado) — `stat`+`shutil.copy2` (NUNCA abre o original), detecta mudança por sha256+size+mtime, arquiva snapshots datados, loga `change_log.csv` com intervalo entre mudanças p/ confirmar os 15 min e horários. Resiliente a share offline/lock. (2) **Desenho técnico do adapter** (CTO) — pacote novo `server/app/erp/` (poller/parse/filter/reconcile/mapping/sink/enrich), worker periódico que só chama `dispatcher.create_task/cancel_task` (igual ao callbutton, zero mudança no caminho de movimento). Tabelas novas `erp_order` (PK `order_key` p/ dedup) + `erp_snapshot` no `fleet.db`. Mapa POU→station via `erp_mapping.yaml`.
- **Decided:** **Idempotência por conteúdo** (order_key = part+local+célula+POU+qtd+data) no DB → dispatcha cada pedido exatamente 1x apesar do feed rolling de 15 min. **Guardrail de ouro: sem mapa POU→landmark → SEM movimento** (`blocked_unmapped`+alarme). Fail-safe no arquivo: stability gate (mtime parado ≥60s) + sanity gates antes de processar; arquivo suspeito = descarta. **Fases:** F1 = dispatcha 1 pedido marcado "AMR" ponta a ponta (parse IKS, filtro marca, dedup, mapa YAML → robô anda; atendimento/cancelamento só logam); F2 = fecha loop (baixa/cancel) + Oracle; F3 = validação de peso.
- **Next:** (a) **Founder roda o watcher** no share real p/ confirmar cadência. (b) Confirmar c/ Cleber/Douglas: token exato da marca "AMR" (97-120), limites do id de 25 díg. (~53-77, sobrepõe fim da qtd — precisa par pedido+atendimento), encoding (cp1252? cp037?), formato/tz da data MM/DD/YYYY. (c) `eng-lead` implementa F1 do adapter (composite key agora, marca plugada depois) — bloqueado no filtro até a marca ser confirmada.
- **Refs:** `scripts/feed_watcher/{feed_watcher.py,README.md}`; desenho do adapter (CTO, design doc); integra em `dispatcher.create_task(pickup_station, dropoff_station)`.

### 2026-06-08 — Demo polish follow-ups: mac spacer, BehaveX, uniform headers
- **Done:** Fixed 3 founder-reported issues (3 parallel streams). (1) **Mac 32px traffic-light spacer** no longer shows on Windows: new `desktop/app/lib/platform.ts` (`isMac()` via `window.electronAPI.platform`, safe/undefined→non-mac in web build); `Layout.tsx` spacer now gated `isMac()`. (2) **"BehaveX" eliminated at runtime**: `electron/main.ts` `app.setName('Caterpillar Inc. Fleet')` + BrowserWindow title; `package.json` name→`caterpillar-fleet`; de-branded `start.bat` + `docs/ACTION_PLAN.md`. Grep clean (only TIMELINE history retains it, intentional). (3) **Uniform page headers**: new shared `PageHeader.tsx` (`min-h-[48px]`, `text-sm font-semibold` title, status/actions/subBar slots) wired into all 7 pages so content baseline starts at same Y on every route; Tasks/Devices secondary rows in a consistent sub-band. Verified lint 0, tsc 0, renderer build green.
- **Decided:** Spacer keyed off real OS platform via preload (not the title-bar style guess). Headers standardized through one component rather than per-page classes.
- **Next:** Founder must **quit and restart `npm run dev`** (Ctrl+C) to see the Electron-runtime fixes (title + spacer need the main process + preload to reload; HMR alone won't). Headers update via HMR.
- **Refs:** `desktop/app/lib/platform.ts` (new), `desktop/app/components/{Layout,PageHeader}.tsx`, all 7 `desktop/app/pages/*.tsx`, `electron/main.ts`, `package.json`, `start.bat`, `docs/ACTION_PLAN.md`.

### 2026-06-08 — Pre-demo polish pass (operator app → pt-BR + branding unified)
- **Done:** Full UI polish sweep on `desktop/` ahead of Marcus's return. (1) **Branding unified to "Caterpillar Inc. Fleet"** everywhere it disagreed: `package.json` productName (was "BehaveX") + appId `com.caterpillar.fleet`, `index.html` title, Layout wordmark + logo glyph "AF"→"CAT". (2) **100% of user-facing text → pt-BR** across all pages (Dashboard/Field/Devices/Calibration/Settings/Tasks/Callbuttons) + Layout nav, STOP-ALL flow (PARAR TUDO/RETOMAR FROTA), toasts, empty/loading/error states, table headers, placeholders; code/identifiers/comments stay English; `index.html lang="pt-BR"`. (3) **Removed/deferred + documented** in new `docs/POST_DEMO_BACKLOG.md` (7 items): hid broken Field "Despachar robô para cá" (malformed task, no backend contract), deleted orphan `figma/ImageWithFallback.tsx`, removed white-flash Devices Refresh, de-duped API (`callbuttonPress`→typed `pressCallbutton`). (4) **Fixes:** scrubbed dev-speak from UI ("Adilson's PLC", "SIM_MODE", "POST /jack"), raw `JSON.stringify`→friendly pt-BR lines, silent `catch{}`→pt-BR toasts, Tasks table shows station labels not IDs, Callbuttons AP1/CB1 empty-state guard, `electron/main.ts` titleBarStyle made mac-only. Verified: lint 0, tsc 0, operator build green.
- **Decided:** Keep product name as **"Caterpillar Inc. Fleet"** for now (per founder). UI fully pt-BR, code English. Prefer hide-and-document over risky refactor for the demo.
- **Next:** Founder owns post-demo items in `docs/POST_DEMO_BACKLOG.md` — chiefly the real robot-pinned dispatch endpoint, favicon asset, Windows traffic-light spacer, task-state badge pt-BR map. Eyeball `npm run dev` before Marcus arrives.
- **Refs:** `package.json`, `index.html`, `electron/main.ts`, `desktop/app/components/Layout.tsx`, `desktop/app/api/fleet.ts`, `desktop/app/pages/{Dashboard,Field,Devices,Calibration,Settings,Tasks,Callbuttons}.tsx`, `desktop/app/components/CallbuttonStationRow.tsx`, deleted `figma/ImageWithFallback.tsx`, new `docs/POST_DEMO_BACKLOG.md`.

### 2026-06-08 — Feed de pedidos DECODIFICADO (layout largura-fixa + campo de observação confirmado)
- **Done:** Engenharia de reversão do `TRANVR2.TXT` (cópia do feed, ~375k linhas, largura fixa 160 chars, atualiza a cada 15 min). **Layout do registro de pedido (IKS), posições 1-indexed:** tipo 1-3 (`IKS`); **part number 7-13** (`3989602`); **local de estoque 15-20** (`BTLOG1`); **célula 30-35** (`BT09TC`); **POU 36-44** (`FLBT10TC3`); quantidade 45-53; data 79-88 (`01/06/2026`); **OBSERVAÇÃO/texto-livre 97-120 (24 chars)**. Tudo bate com o exemplo decodificado pelo founder. Tipos de registro confirmados: Pedidos (IKS/I5F/N5F/NFM/NKS/NS3), Atendimento (I6A/N6A/N6K/N6L), Cancelamento (I7Q/N02/N7A/N7V) — **e MUITOS outros não listados pelo Douglas** (I5U ~85k, I6F ~57k, N5U ~25k, etc.); doc dele está incompleta.
- **Decided:** **3 achados que destravam o PoC:** (1) **Campo de observação 97-120 está EM BRANCO nos pedidos** e aparece **preenchido com texto livre nos atendimentos** (`DOC 706.412`, `PAULO`, `ML042025`) → confirma que é texto livre real → a marca "AMR" do Zampim cai aí e o parser lê por posição fixa. **Plano da marca validado.** (2) **Junção pedido→atendimento→cancelamento** provável por chave **part+local+célula** (há também bloco numérico 25 díg. em ~53-77 candidato a ID de pedido) → fecha o loop (cria/baixa/cancela) sem write-back. (3) Filtrar só os marcados "AMR" isola o escopo independ. do tamanho do feed.
- **Next:** Confirmar c/ Cleber/Douglas: (a) amostra de txt JÁ com 1 PN marcado, p/ validar posição da marca na observação do registro de **pedido**; (b) chave de junção definitiva (part+local+célula vs. nº de pedido único); (c) encoding + o que é o `####` do nome do arquivo. Depois: `cto-architect` escopa o adapter (file-watcher → copia, nunca abre o original; parse largura-fixa; filtra marca AMR; → dispatch existente) + Oracle read-only de enriquecimento.
- **Refs:** `TRANVR2.TXT` (cópia local), `image.png` (tela BlueZone 73J IDENT INVENTORY INQUIRY); segue a entrada "Fonte de dados ACHADA".

### 2026-06-08 — Alinhamento com Zampim: Fleet RDS p/ teste + marca de "AMR" na observação do pedido
- **Done:** Founder explicou todo o desenho (feed txt + Oracle + loop puxado) ao Zampim. Ações combinadas: (1) **solicitar licença do Fleet RDS** para testar a stack contra o ambiente SEER real; (2) **marcar só alguns part numbers** como elegíveis a AMR — sempre que for um deles, a **observação do pedido** sinaliza que o transporte deve ser feito pelo AMR.
- **Decided:** **Roteamento por flag na observação do pedido**, não por lista mantida pela gente — a fonte da verdade fica no sistema da CAT; nosso adapter só filtra "tem a marca AMR? → dispatcha". Zero lista paralela p/ desatualizar; escopo do PoC controlado por cadastro (marcar/desmarcar PN), não por código. Fleet RDS = ambiente de teste antes do chão.
- **Next:** Confirmar (a) **posição fixa exata** da observação/marca no txt feed — pedir amostra de txt JÁ com a marca aplicada + etiqueta; (b) **string/código exato** da marca (único e estável, ex. `AMR`); (c) quem aplica e se persiste em todo pedido do PN; (d) prazo da licença Fleet RDS. Depois: `cto-architect` escopa o adapter (filtra pela marca + parse posicional célula/POU/PN) → dispatch.
- **Refs:** conversa Zampim; segue a entrada de hoje "Fonte de dados do pedido ACHADA".

### 2026-06-08 — Fonte de dados do pedido ACHADA (txt feed + Oracle): caminho de integração do PoC
- **Done:** Founder foi à logística. Lele (não soube) → encaminhou ao **Douglas Gonçalves**, que entregou as fontes reais: (1) **Feed de pedidos em txt** `\\b8nas01.brazil.cat.com\accfiles\B8HK\####.txt` — MESMA fila que alimenta a impressora de etiquetas; contém **célula + ponto de uso** por pedido. Linhas têm códigos de 3 chars para **Pedidos** (I5F/IKS/N5A/N5F/NFM/NKS/NS3), **Cancelamento** (I7Q/N02/N7A/N7V), **Atendimento** (I6A/N4I/N6A/N6J/N6K/N6L/N7J). (2) Tabelas **Oracle**: `B8HJ_PONTO_USO_PECA` (prédio/célula/POU) e `B8HJ_PES_PESO` (peso da peça). Contato p/ integração DOS: **Cleber Movio**. Também: a CAT está implantando o **DOS** (tablet p/ o empilhaderista) — **Fábio** (manager) tem ressalva quanto ao AMR adicionar complexidade num processo que já vai mudar; founder explicou que é PoC.
- **Decided:** **Usar o `####.txt` como gatilho do PoC, em modo só-leitura** — é exatamente o dado da etiqueta, em tempo real, com os 3 eventos (pedido→cria job / cancelamento→cancela / atendimento→baixa), fechando o loop sem write-back. **Oracle = enriquecimento read-only** (POU autoritativo + peso p/ validar carga). **Postura: observador passivo, não-invasivo, desligável** → não muda o processo deles nem colide com o DOS; defende contra a ressalva do Fábio e o DOS valida que o feed é estável o bastante p/ construir em cima.
- **Next:** Perguntas a fechar — **Cleber:** layout exato do txt (posições de célula/POU/PN/qtd/ts/nº pedido), o que é `####`, append-only?, encoding, acesso de leitura ao share na rede do piloto, amostra (1 txt + 1 etiqueta). **Douglas:** schema/chave de `B8HJ_PONTO_USO_PECA`, usuário Oracle read-only. **Fábio:** cronograma do DOS na célula, objeção real, esperar vs. paralelo. Depois: `cto-architect` escopa o adapter file-watcher (parse txt) + consulta Oracle → alimenta o dispatch existente.
- **Kill criteria:** sem acesso de leitura ao share/Oracle na rede do piloto, ou Fábio bloqueia a célula → cai p/ plano-ponte (humano digita destino na nossa tela) p/ provar navegação+jack+loop sem integração.
- **Refs:** conversa logística (Lele→Douglas Gonçalves→Cleber Movio / Fábio); segue o estudo de fonte de dados das entradas anteriores de hoje.

### 2026-06-08 — Fluxo de reposição da linha de montagem (transmissão): plano do MVP
- **Done:** Founder + Zampim mapearam (qua) o fluxo manual ATUAL da linha de transmissão: montador solicita peça na "tela preta" (ERP) → logística imprime chamados (célula, ponto de uso, endereço no estoque) → empilhadeirista pega, entrega e dá baixa via "zebra" no destino. Domain-expert + PM desenharam o MVP AMR-based.
- **Decided:** (1) **NÃO dar baixa no carregamento** — quebra rastreabilidade (material ainda não está no ponto de uso). Status separados: **staged → loaded → entregue (confirmado no destino = nossa baixa) → vazio retornado**. (2) Duas áreas (ENVIO/RECEBIMENTO) **não bastam** — precisa de **drop bay fixo por célula** + **ponto de espera (hold)** do AMR p/ destino bloqueado. (3) Reusar a página **Tasks** com um painel "Assembly Loop" (Full Rack: ENVIO→ponto de uso / Empty Return: ponto de uso→RECEBIMENTO); confirmação manual no destino = baixa no NOSSO sistema, sem integração profunda com o ERP. (4) **Empilhadeirista/separador continua no v1** — AMR substitui o transporte, não o pick nem o load/unload (ainda).
- **Next:** Founder validar com Zampim: quem confirma no destino (montador vs. logística), tablet no ponto de uso vs. desk central, lista dos primeiros pontos de uso, 1 rack por viagem, jack up/down no fluxo padrão? Depois: eng-lead implementa Assembly Loop + lifecycle do job.
- **Refs:** field visit (qua); segue decisão de 2026-06-03 (linha de montagem = input via interface web/desktop).
